spring:
  datasource:
    username: postgres
    password: lol
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://structuremade.de:5432/structuremade
  jpa:
    show-sql: on
    generate-ddl: on
    properties:
      hibernate:
        default_schema: services
  application:
    name: ACTIVITYDELETINGSERVICE
server:
  port: 8080
